# Backup Slots

This page documents `infomaniak/resources/backup/slots.py`.

## Methods

- `list()`
- `create()`
- `disable()`
- `enable()`
- `display()`
- `update()`
- `delete()`
- `rclone()`

## Request models

- Placeholder inline payloads (to be aligned with OpenAPI once generated).

## Returned models

- `BackupSlot`

## Status

All methods are currently placeholders and raise `NotImplementedError`.
