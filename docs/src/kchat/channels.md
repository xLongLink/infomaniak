# kChat Channels

Placeholder resource for `client.kchat.channels`.

## Methods

- `list()`, `create()`, `create_direct_message()`, `create_group_message()`
- `search_all()`, `search_group()`, `search()`, `search_archived()`
- `by_ids()`, `display()`, `update()`, `delete()`, `patch()`, `restore()`, `move()`
- `stats()`, `pinned_posts()`, `public()`, `private()`, `deleted()`
- `autocomplete()`, `autocomplete_search()`
- `by_name()`, `by_name_and_team_name()`
- `view()`, `memberships_and_roles()`, `for_user()`, `all()`, `unread_messages()`
- `privacy.update()`
- `members.list()`, `members.add()`, `members.by_ids()`, `members.display()`, `members.remove()`
- `members.roles.update()`, `members.scheme_roles.update()`
- `notifications.update()`
- `scheme.set()`
- `moderation.get()`, `moderation.update()`
- `sidebar.categories.list()`, `sidebar.categories.create()`, `sidebar.categories.update()`
- `sidebar.order.get()`, `sidebar.order.update()`
- `sidebar.category.display()`, `sidebar.category.update()`, `sidebar.category.delete()`

## Status

All methods currently raise `NotImplementedError` placeholders.
