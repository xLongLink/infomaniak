# URL Short

The `short` resource groups URL shortener endpoints.

## Methods

- `client.url.short.list()`
- `client.url.short.create()`
- `client.url.short.update()`
- `client.url.short.quota()`

## Status

All current methods are placeholders and raise `NotImplementedError`.
